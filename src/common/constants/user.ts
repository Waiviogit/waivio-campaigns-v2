export const REFERRAL_TYPES = Object.freeze({
  REWARDS: 'rewards',
});

export const REFERRAL_STATUSES = Object.freeze({
  NOT_ACTIVATED: 'notActivated',
  ACTIVATED: 'activated',
  REJECTED: 'rejected',
});

export const REWARD_SETTINGS = Object.freeze({
  SP: 'SP',
  FIFTY_FIFTY: '50',
  STEEM: 'STEEM',
});

export const USER_PROVIDE = Object.freeze({
  REPOSITORY: 'UserRepository',
});
